module.exports = function (req, res, next){
	var list = new list(req._parsedUrl, req.params);
	var body = list.get();
	res.setHeader('Content-Length', body.length);
	res.setHeader('Content-Type', 'text/html');
	res.end(body);
	//console.log('--- rrr', req._parsedUrl);
	//res.end('fdsfdsfsd');
};


var list = function (url, params){
	this.url = url;
	this.params = params;
};

list.prototype.