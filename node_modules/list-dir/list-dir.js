var express = require('express');
var request = require('./request');

module.exports = {
	listen: function(){
		var app = express();
		app.all('*', request);
		app.listen.apply(app, arguments)
	}
};

